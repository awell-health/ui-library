import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button } from './Button';

<Meta title="atoms/Button" component={Button} />


# Button

<Canvas>
  <Story name="Button">
    <form>
      <div>
        <h3>Primary button</h3>
        <Button onClick={() => null}>Primary</Button>
      </div>
      <div>
        <h3>Secondary button</h3>
        <Button variant="secondary" onClick={() => null}>Secondary</Button>
      </div>
      <div>
        <h3>Text button</h3>
        <Button variant="text" onClick={() => null}>Text</Button>
      </div>
    </form>
  </Story>
</Canvas>

### Component usage
```js
<Button variant='secondary' onClick={() => null}>Hello world!</Button>
```

